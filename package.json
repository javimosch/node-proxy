{
  "name": "node-proxy",
  "version": "1.0.0",
  "description": "Node.js/Express application that acts as a reverse proxy with React UI",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"nodemon index.js\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "start": "node index.js",
    "populate-db": "node scripts/populate-db.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "http-proxy-middleware": "^3.0.3",
    "mongoose": "^8.12.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.9"
  }
}
